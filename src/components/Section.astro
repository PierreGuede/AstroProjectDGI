---
const { bk, tl, ch, se, title } = Astro.props;
---

<h5 id={`bk${bk}-tl${tl}-ch${ch}-se${se}`} class="section">SECTION {se} : {title}</h5>

<style>
  .section {
    font-size: 1rem;
    font-weight: 600;
    padding-left: 1em;
    margin-bottom: 2rem;
    text-transform: uppercase;
  }
</style>
